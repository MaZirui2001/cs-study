diff a/SysYF_Backend_Student/test/Test_H/Medium_H/assign_after_continue.sy b/SysYF_Backend_Student/test/Test_H/Medium_H/assign_after_continue.sy	(rejected hunks)
@@ -5,7 +5,7 @@ int main(){
     int cnt = 0;
     while (a < arr[a]) {
         a = arr[a];
-        if (arr[a + arr[a] - 1] > arr[a] - 1.5) {
+        if (arr[a + arr[a] - 1] > arr[a] - 2) {
             continue;
             cnt = arr[cnt] + 1;
         }
