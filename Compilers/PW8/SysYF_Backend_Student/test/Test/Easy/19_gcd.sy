int n;

int gcd(int m,int n)
{ 
    int t;
    int r;

    if(m<n) { t=m;m=n;n=t; }

    r=m%n;

    while(r!=0)
    {
        m=n;   
        n=r;   
        r=m%n;  
    }

  return n;

}

int main()
{
        int start = currenttime();

    //newline=10;
    int i;
    int m;
    //m = 1478;
    //int t;
    i=getint();
    m=getint();
    int tmp = gcd(i, m);
        int end = currenttime();
        putch(10);
    putint(end - start);
    return tmp;
}