int f(int a, int b) {
	return a * b;
}

int g(int a, int b) {
	return a % b;
}

int h(int a, int b) {
	return f(f(2, g(a, b)), g(f(a, b), 4));
}

int main () {
	    int start = currenttime();

	putint(h(11, 3));    
	int end = currenttime();
	putch(10);
    putint(end - start);
	return 0;
}